# OrbisView gflags (optional --flagfile=…).
# Mirrors Dreamview conf/dreamview.conf layout; values match main.cpp defaults.

--host=0.0.0.0
--port=8766
# Live Autolink (autosim / stack). Use --mock=true --autolink=false for offline UI.
--mock=false
--autolink=true
# After CMake build: frontend/dist; after install: share/autonomy/orbisview/www
--document_root=
--plugin_dir=
--cmd_vel_channel=/cmd_vel
--goal_pose_channel=/goal_pose
--goal_poses_channel=/goal_poses
--cancel_navigation_channel=/cancel_navigation

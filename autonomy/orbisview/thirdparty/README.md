# OrbisView third-party

| Path | Role | Built? |
|------|------|--------|
| `civetweb/` | HTTP/WebSocket transport (MIT) | Yes (`add_subdirectory`) |

See `civetweb/ORIGIN.md` and `civetweb/LICENSE.md`.

Apollo Dreamview / Dreamview Plus are **not** vendored here; use upstream Apollo as external reference only.

# System Monitor

```bash
autonomy.monitor --conf=monitor.pb.txt
# loads autonomy/system/conf/monitor.pb.txt (AUTONOMY_PATH / AUTONOMY_CONF_PATH)
```

Edit `autonomy/system/conf/monitor.pb.txt` (protobuf text).

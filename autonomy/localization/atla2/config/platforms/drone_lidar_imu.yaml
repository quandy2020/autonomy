# Atla2 platform: lidar-inertial
frontend:
  mode: lio
  fusion: loose
  vio:
    enabled: false
  lio:
    enabled: true
    lidar: mid360
  imu: default

sensor:
  gps:
    enabled: false

backend:
  type: iekf

map:
  max_local_map_points: 80000
  voxel_size: 0.2

sync_tol_ms: 20.0

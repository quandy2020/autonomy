<!--
  Teleop: publish while watchdog holds. Perception / MPPI assist is handled inside
  TrackCommand (passthrough when sensors or paths are not ready).
-->
<root BTCPP_format="4" main_tree_to_execute="Teleop">
  <BehaviorTree ID="Teleop">
    <KeepRunningUntilFailure>
      <ReactiveSequence name="Tick">
        <CommandValid/>
        <TrackCommand error_code_id="{teleop_error_code}"
                      error_msg="{teleop_error_msg}"/>
      </ReactiveSequence>
    </KeepRunningUntilFailure>
  </BehaviorTree>
</root>

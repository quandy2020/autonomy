# Atla2 platform: monocular visual odometry (no IMU)
frontend:
  mode: vo
  vo:
    enabled: true
    camera: front
  vio:
    enabled: false
  lio:
    enabled: false

sensor:
  gps:
    enabled: false

backend:
  type: iekf

map:
  max_local_map_points: 20000
  voxel_size: 0.2

sync_tol_ms: 20.0

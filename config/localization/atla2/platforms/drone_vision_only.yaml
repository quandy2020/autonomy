# Atla2 platform: vision-inertial only
frontend:
  mode: vio
  fusion: loose
  vio:
    enabled: true
    camera: front
  lio:
    enabled: false
  imu: default

sensor:
  gps:
    enabled: false

backend:
  type: iekf

map:
  max_local_map_points: 20000
  voxel_size: 0.2

sync_tol_ms: 20.0

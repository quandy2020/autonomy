---
# Local / single-host inventory (see staging/ and production/ for fleet deploys)
all:
  children:
    autonomy:
      hosts:
        localhost:
          ansible_connection: local
          ansible_python_interpreter: /usr/bin/python3

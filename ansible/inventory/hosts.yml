---
# 示例 inventory：复制并修改 ansible_host / ansible_user。
all:
  children:
    autonomy:
      hosts:
        robot01:
          ansible_host: 192.168.1.100
          ansible_user: ubuntu
        # 本机调试（控制机即目标机）：
        # localhost:
        #   ansible_connection: local

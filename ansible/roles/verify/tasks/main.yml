---
- name: Wait for enabled autonomy services to become active
  ansible.builtin.systemd:
    name: "{{ item }}"
  register: autonomy_service_status
  until: autonomy_service_status.status.ActiveState == 'active'
  retries: 10
  delay: 3
  loop: "{{ autonomy_enabled_units }}"
  become: true

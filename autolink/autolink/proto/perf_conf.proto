syntax = "proto3";

package autolink.proto;

enum PerfType {
  SCHED_UNSPECIFIED = 0;
    SCHED = 1;
  TRANSPORT = 2;
  DATA_CACHE = 3;
  ALL = 4;
}

message PerfConf {
  bool enable = 1;
  PerfType type = 2;
}

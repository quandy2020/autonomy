---
description: 机器人学算法/规划文档写作规范。参照 GL Robotics 轨迹优化博文与 TEB 控制器范例（14_teb_controller.md）：论文公式编号、分层主问题、公式后释义。禁止假设节、符号表、Challenges、(P1) 自定义编号。
globs: docs/source/**/*.md,**/*.tex
alwaysApply: true
---

# Role: 机器人学规划与控制文档写作规范

撰写 `docs/source` 下控制器、规划器、SLAM 等**算法文档**时，参照：

- [GL Robotics · 轨迹优化系列](https://gl-robotics.com/blog/轨迹优化/)：**动机 → 分步推导 → 符号释义 → 约束分层**
- **范例模板**：`docs/source/09_Control/controller/14_teb_controller.md`（TEB，IROS 2017 + ECMR 2013）

**目标**：一条数学主线、与主文献公式编号一致、无实现细节堆砌；读者能回答「为什么这样建模」与「每个符号代表什么」。

---

## 1. 禁止项（硬性）

| 禁止 | 说明 |
|------|------|
| **Assumption / 假设** | 不写 Assumption 1,2,…；理想化条件一句并入正文或 `s.t.` |
| **引言** | 不写「§X.1 引言」「本节给出…」；章首直接进内容 |
| **解的性质** | 不写存在性/唯一性专节；非凸、局部最优等一句写在 §5 |
| **本文结构 / 章节导航表** | 文首仅 blockquote 摘要，不写目录表 |
| **方法对比表** | 不写 TEB vs DWB/MPPI 等选型表 |
| **符号表 / Nomenclature** | 符号在公式下方 bullet 释义 |
| **(P1) / (P1a) 自定义编号** | 用主文献式号（如 IROS 式 (1)、式 (NLP)、式 (13)–(14)） |
| **典型结果 / Challenges** | 不写论文实验复现节、open challenges |
| **代码 / 参数表** | 不写 Edge 类名、`weight_*`、ROS 参数表（除非用户明确要求） |
| **重复抄写主问题** | §4.2 主问题写一次；§5 用等价变换，不全文再抄 |

---

## 2. 文档级骨架（六段式）

| 顺序 | 章节 | 内容 |
|------|------|------|
| 1 | **背景 §1** | 领域痛点、方法由来（1–2 段）；不写任务形式化 |
| 2 | **问题 §2** | **任务.** / **输入·输出.** / **在线形式.**（粗体标签 + 短 prose） |
| 3 | **运动模型 §3** | 连续/离散运动学 → 轨迹参数 → 导出量；章首一句路线图 |
| 4 | **数学问题定义 §4** | §4.1 硬约束 NLP → §4.2 软约束主问题（罚函数）；章首一句说明分层 |
| 5 | **求解 §5** | 算法框 → WNLS 等价 → 稀疏 → LM 与线性子问题 |
| 6 | **参考文献 §6** | 主文献 PDF/DOI 编号列表 |

**文首**：blockquote 摘要（方法名 + 核心机制 + 在线形式，2–3 句）。

**§2 不写** OCP/NLP/WNLS 三层 bullet 枚举；层级在 §4–§5 自然展开。

---

## 3. 与主文献对齐（公式）

1. **式号引用**：正文写「IROS 式 (7)」「ECMR 2013 Fig. 2」，不发明新编号。
2. **符号一致**：优先论文符号（如 $\tilde{r}_k$、$\phi$/$\chi$、$\tilde{V}(B)$、$B^*$）；不自创 $\Sigma$、$\mathbf{u}$ 堆叠除非论文确有。
3. **简洁优先**：论文为标量则写标量；向量/矩阵仅在有物理堆叠时写（如 $\mathbf{o}_k$、$\boldsymbol{\nu}_k$）。
4. **近似并列**：精确式与实现近似同一行（如 $\rho_k=\cdots\approx\cdots$），与 IROS 式 (5)(7) 同型。
5. **不堆砌等价改写**：同一公式不写 $B(\beta)$、反对称矩阵、$\tau$ 列向量等多种改写；选论文最简形式一种。

---

## 4. 公式与排版

### 4.1 小节结构（§3 / §5）

- **`### 3.x` / `### 5.x`**：一级推导步骤。
- **`**粗体短语.**`**：块内小标题（如 **连续运动学**、**惩罚函数**、**滚动边界.**），后接公式。
- **流程**：1 句 prose（为何需要）→ 独立 `$$…$$` → 2–4 条 bullet 释义（含 **含义** 若必要）。

### 4.2 释义 bullet

- 格式：**$符号$**：物理含义。
- 每个**重要公式块**后必有释义；不必逐符号列表。
- 奇异性（$v=0$、$\mathbf{d}_k=\mathbf{0}$）inline 一句，不单开小节。

### 4.3 LaTeX 习惯

- 范数：`\lVert\cdot\rVert_2`；优化：`\underset{}{\arg\min}`。
- `aligned` 内不写 `\tag{(P1)}`；`s.t.` 与目标同一 `aligned` 块。
- 公式块末要有标点（中文语境下逗号/句号与论文一致）。

### 4.4 插图

- 路径：`../images/<topic>/`；**标准 Markdown** `![alt](../images/...png)`（不用 `` ```{figure} ``，保证 IDE 预览）。
- 图下：*斜体 caption*，注明论文 Fig. 编号与图中要点。
- 位置：相关 prose **之后**、下一小节公式 **之前**；§5 开篇可放控制流总图。
- 显示尺寸：由 `docs/source/custom.css` 按文件名控制，正文不写 `{width=…}`。

---

## 5. §4 数学问题（分层模板）

**§4 开篇**（1 段）：$B$ 已参数化 → §4.1 全硬约束 NLP → 在线难解 → §4.2 罚函数松弛为软约束。

### 5.1 §4.1 NLP

- 标题：**IROS 式 (NLP)** 或论文等价名。
- 内容：`min` + `s.t.` 一块 `aligned`；约束用论文函数签名 $\mathbf{h}_k(s_{k+1},s_k)$ 等。
- 复杂约束（障碍、速度界）可另起一块公式 + 1–2 bullet。
- **含义** bullet：目标物理意义 + 为何需软约束/罚函数近似。

### 5.2 §4.2 软约束主问题

三块，不拆更多：

1. **保留的硬约束.** — 边界 + 时间界留在 `s.t.`。
2. **软约束化.** — 论文 $\phi$（等式罚）、$\chi$（不等式 `min{0,g}` 单侧罚）；各 1 行公式 + 1 bullet。
3. **主问题** — 论文 $\tilde{V}(B)$ 或 $\sum f_i^2$ 展开；$\arg\min$ 写 $B\setminus\{s_1,s_n\}$ 若论文如此。
4. **权重 / 含义** — $\sigma_h$ 量级一句；$\sigma\to\infty$ 与 NLP 关系一句。
5. **ECMR 扩展**（可选 1 bullet）— path / waypoint 以同型 $\chi$ 加入 $\mathbf{r}$；不展开 Edge 列表。

**不写**单独「残差项 $f_{\cdot,k}$ 长列表」若 $\tilde{V}$ 已含 $\phi$/$\chi$；WNLS 残差栈留 §5.2 简述。

---

## 6. §5 求解（模板）

**§5 开篇**（1 段）：$\tilde{V}$ ↔ WNLS、算法 1–3 给出单周期流程。

| 小节 | 内容 |
|------|------|
| **5.1 算法（数学描述）** | `algorithm-box`：TEB 主流程 + `SolveWNLS` + `MapControl`（LaTeX/英文步骤，中文仅注释） |
| **5.2 WNLS 形式** | $\tilde{V}\Leftrightarrow\tfrac12\lVert\mathbf{r}\rVert^2$；IROS $\phi$/$\chi$ + ECMR path/waypoint |
| **5.3 稀疏结构** | 局部依赖 bullet → $J,H$ → 超图 Fig. → **例外**（稠密块） |
| **5.4 LM 与线性子问题** | $I_{\mathrm{LM}}$ 步 LM；$(H+\lambda I)\Delta B=-\mathbf{g}$；AMD + Cholesky；Fig. 4 |

滚动输出（$\mathbf{u}_1$ + warm-start）写入算法 1 footer（英文为主 + 中文括号注释），不单开节。

非凸、局部最优、$O(n_b)$：**不单开节**，写入对应 bullet。

---

## 7. §3 运动模型（模板）

章首 1 句：§4 所需链条（如 连续 → 离散 → $B$ → 段速度）。

| 小节 | 典型内容 |
|------|----------|
| 3.1 | $\{W\}$、$s(t)$、式 (1)(2)、与优化变量关系 1 句 |
| 3.2 | 离散几何、共弧、$\tilde{r}_k$；配 Fig. 1 |
| 3.3 | $B$ 定义式 (6)、滚动边界；配 TEB 定义 Fig. |
| 3.4 | 段 $v_k,\omega_k,a_k$ 式 (7)–(9)；多平台可 Car-like 主写 + 差速一句 |

每小节末：**与 §4/§5 的衔接**可 inline（稀疏结构、约束来源），不重复 §4 全文。

---

## 8. 写作红线（检查清单）

- [ ] 无物理定义直接写 $\min\sum f_i^2$
- [ ] 公式与主文献式号、符号一致
- [ ] 无符号表、无 (P1) 标签、无 Challenges/典型结果
- [ ] 重要公式后有 bullet 释义
- [ ] §4.1 NLP 与 §4.2 $\tilde{V}$ 各写一次，不第三处重复
- [ ] 插图 Markdown + caption，非 code block
- [ ] 口语类比（「橡皮筋」等）全文最多 **一处**
- [ ] 段落间空一行；控制器单篇 **250–350 行**

---

## 9. 输出指令

| 用户意图 | 动作 |
|----------|------|
| 新建/重写控制器文档 | 按 [§2 六段式](#2-文档级骨架) + [§5–§7 模板](#5-4-数学问题分层模板)；对照 `14_teb_controller.md` |
| 参考论文梳理 | §1 背景 → §2 问题 → §3 运动模型 → §4 NLP+软约束 → §5 求解 → §6 文献 |
| 公式修订 | 先查 PDF 式号与符号，再改 prose；删等价改写，保简洁 |
| 插图 | 论文 Fig. 截取至 `docs/source/.../images/`；caption 写 Fig. 编号 |

**语调**：学术、可读；中文为主；**先讲清为什么，再写公式**；**论文为准，简洁易懂**。
